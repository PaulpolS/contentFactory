"import React, { useState, useEffect, useRef } from 'react';\nimport { \n  Compass, \n  Database, \n  Image as ImageIcon, \n  Terminal, \n  Play, \n  CheckCircle, \n  Sliders, \n  Search, \n  Info, \n  Layers, \n  Tv, \n  Star,\n  ExternalLink,\n  ChevronRight,\n  TrendingUp,\n  Cpu,\n  RefreshCw,\n  Eye\n} from 'lucide-react';\n\nconst API_BASE = 'http://localhost:5005/api';\n\ninterface VaultContent {\n  id: string;\n  source_type: 'radar' | 'rss' | 'youtube' | 'github';\n  title: string;\n  selected_headline: string | null;\n  raw_content: string;\n  source_url: string;\n  author_name: string | null;\n  author_avatar_url: string | null;\n  author_followers: number | null;\n  rating_news: number;\n  rating_evergreen: number;\n  metadata: any;\n  media_paths: string[];\n  status: 'scraped' | 'ready_for_design' | 'designed' | 'posted' | 'archived';\n  created_at: string;\n  updated_at: string;\n}\n\ninterface GraphicItem {\n  id: string;\n  content_id: string;\n  file_path: string;\n  image_ratio: string;\n  theme_name: string;\n  created_at: string;\n}\n\nexport default function App() {\n  const [activeTab, setActiveTab] = useState<'discovery' | 'vault' | 'canvas'>('discovery');\n\n  // Vault data states\n  const [vaultItems, setVaultItems] = useState<VaultContent[]>([]);\n  const [selectedItem, setSelectedItem] = useState<VaultContent | null>(null);\n  const [loadingItems, setLoadingItems] = useState(false);\n\n  // Vault filters state\n  const [filterSource, setFilterSource] = useState<string>('all');\n  const [filterStatus, setFilterStatus] = useState<string>('all');\n  const [filterKeyword, setFilterKeyword] = useState<string>('');\n  const [filterMinRating, setFilterMinRating] = useState<number>(0);\n  const [sortBy, setSortBy] = useState<string>('newest');\n\n  // Discovery Portal input states\n  const [radarUrl, setRadarUrl] = useState('https://www.facebook.com/techfeedthailand');\n  const [radarLimit, setRadarLimit] = useState(10);\n  const [rssUrl, setRssUrl] = useState('https://techcrunch.com/feed/');\
<truncated 59499 bytes>